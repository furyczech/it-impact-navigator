# Supabase Configuration
VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}

# Database Connection (replace [YOUR-PASSWORD] with your actual password)
DB_USER=postgres
DB_PASSWORD=${DB_PASSWORD}  # Replace with your Supabase database password
# Use IPv6 address directly (enclosed in square brackets for IPv6)
DB_HOST=2a05:d014:1c06:5f14:9f9c:da0c:2a2f:ab20
DB_PORT=5432
DB_NAME=postgres

# Connection URL (for reference, used in some tools)
DATABASE_URL=${DATABASE_URL}

# Local Development (optional)
VITE_API_URL=http://localhost:4000
